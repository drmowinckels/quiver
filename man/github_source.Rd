% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source-github.R
\name{github_source}
\alias{github_source}
\title{Reference a 'GitHub' repository as a skill source}
\usage{
github_source(repo, ref = NULL)
}
\arguments{
\item{repo}{\code{"owner/repo"} slug of the source repository.}

\item{ref}{Git ref (tag, branch, or SHA) to install from. Defaults to
the latest 'GitHub' release, falling back to the default branch if the
repo has no releases. Resolved once per source and reused across
calls, so every skill installed from a single \code{\link[=quiver_install]{quiver_install()}} call
comes from the same commit.}
}
\value{
A \code{github_source} object, for use with \code{\link[=quiver_install]{quiver_install()}}.
}
\description{
Treats \code{repo} as a 'GitHub'-hosted skills repository: any \code{SKILL.md}
found anywhere in the tree is an installable skill, named after its
parent directory. This covers both a flat \verb{skills/<name>/SKILL.md}
layout and a categorised layout like \verb{<category>/<name>/SKILL.md}.
}
\examples{
\dontrun{
source <- github_source("rladies/grimoire")
quiver_install("rladies-blog-post", source, agent = "claude")
}

}
