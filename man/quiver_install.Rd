% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quiver-install.R
\name{quiver_install}
\alias{quiver_install}
\title{Install a skill from a source}
\usage{
quiver_install(
  skill,
  source,
  agent = quiver_agents(),
  scope = c("project", "user"),
  path = ".",
  force = FALSE
)
}
\arguments{
\item{skill}{Character vector of skill names available in \code{source},
e.g. the names of the subdirectories under \verb{skills/} for a
\code{\link[=local_source]{local_source()}}.}

\item{source}{A skill source, e.g. created with \code{\link[=local_source]{local_source()}}.}

\item{agent}{Which agent to install for; see \code{\link[=quiver_agents]{quiver_agents()}}.}

\item{scope}{\code{"project"} installs under \code{path}; \code{"user"} installs into
the agent's directory in the user's home, regardless of \code{path}.}

\item{path}{Project directory to install into when \code{scope = "project"}.
Defaults to the current directory.}

\item{force}{If \code{FALSE} (the default), an already-installed skill is left
as-is. If \code{TRUE}, it is deleted and re-installed, discarding any local
edits.}
}
\value{
The path(s) to the installed skill folder(s), invisibly.
}
\description{
Copies one or more skills from \code{source} into the directory the given
\code{agent} reads skills from, at project or user scope. Already installed
skills are left untouched unless \code{force = TRUE}.
}
\examples{
checkout <- tempfile("quiver-checkout")
dir.create(file.path(checkout, "skills", "demo-skill"), recursive = TRUE)
writeLines(
  c("---", "name: demo-skill", "description: Demo skill.", "---"),
  file.path(checkout, "skills", "demo-skill", "SKILL.md")
)

source <- local_source(checkout)
project <- tempfile("quiver-project")
dir.create(project)

quiver_install("demo-skill", source, agent = "claude", path = project)

}
