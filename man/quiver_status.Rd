% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quiver-status.R
\name{quiver_status}
\alias{quiver_status}
\title{Report the status of installed skills}
\usage{
quiver_status(
  agent = quiver_agents(),
  scope = c("project", "user"),
  path = "."
)
}
\arguments{
\item{agent}{Which agent to install for; see \code{\link[=quiver_agents]{quiver_agents()}}.}

\item{scope}{\code{"project"} installs under \code{path}; \code{"user"} installs into
the agent's directory in the user's home, regardless of \code{path}.}

\item{path}{Project directory to install into when \code{scope = "project"}.
Defaults to the current directory.}
}
\value{
A data frame with columns \code{skill} and \code{modified}, invisibly.
}
\description{
Prints, and invisibly returns, which skills are installed for a given
agent and scope, and whether any have been hand-edited since install.
}
\examples{
checkout <- tempfile("quiver-checkout")
dir.create(file.path(checkout, "skills", "demo-skill"), recursive = TRUE)
writeLines(
  c("---", "name: demo-skill", "description: Demo skill.", "---"),
  file.path(checkout, "skills", "demo-skill", "SKILL.md")
)

source <- local_source(checkout)
project <- tempfile("quiver-project")
dir.create(project)
quiver_install("demo-skill", source, agent = "claude", path = project)

quiver_status(agent = "claude", path = project)

}
